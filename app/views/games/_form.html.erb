<%= form_for(@game) do |f| %>
    <% if @game.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

          <ul>
            <% @game.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    Whites: <%= f.collection_select :whites_id, Player.all, :id, :code, prompt: true %><br>
    Blacks: <%= f.collection_select :blacks_id, Player.all, :id, :code, prompt: true %>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
